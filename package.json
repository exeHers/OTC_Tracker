{
  "name": "otc-trading-tracker",
  "version": "1.0.0",
  "description": "OTC 5-Second Trading Tracker — Underground Cyberpunk (mobile app)",
  "scripts": {
    "build": "echo No build step - www/ is static",
    "cap:sync": "npx cap sync && node scripts/fix-capacitor-flatdir.js",
    "cap:sync:android": "npx cap sync android && node scripts/fix-capacitor-flatdir.js",
    "cap:open:android": "npx cap open android"
  },
  "capacitor": {
    "platforms": ["android"]
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "private": true
}
